// Import the Home component from your `home` page
import Home from './(pages)/home/page';

export default function Page() {
  return (
    <div>
      {/* Render the Home component */}
      <Home />
    </div>
  );
}
